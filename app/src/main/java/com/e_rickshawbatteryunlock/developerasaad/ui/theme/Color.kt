package com.e_rickshawbatteryunlock.developerasaad.ui.theme

import androidx.compose.ui.graphics.Color

// ─── Brand Palette ────────────────────────────────────────────────────────────

/** Electric teal — primary action colour. */
val ElectricTeal = Color(0xFF00C48C)

/** Darker teal used for containers and pressed states. */
val TealDark = Color(0xFF00896A)

/** Teal container for chips and surface containers. */
val TealContainer = Color(0xFF00543E)

/** Deep navy — application background in dark theme. */
val DeepNavy = Color(0xFF0A0E1A)

/** Surface colour — cards and elevated surfaces. */
val SurfaceDark = Color(0xFF111827)

/** Surface variant — input fields and secondary surfaces. */
val SurfaceVariantDark = Color(0xFF1E2A3A)

/** Outline colour for borders and dividers. */
val OutlineDark = Color(0xFF2D3F55)

// ─── Semantic Colours ─────────────────────────────────────────────────────────

/** Amber — used for warnings (high temperature, low SoC). */
val Amber = Color(0xFFFFB347)
val AmberContainer = Color(0xFF5C3800)

/** Error red — used for failures and critical alerts. */
val ErrorRed = Color(0xFFFF5252)
val ErrorContainer = Color(0xFF5C0000)

/** Success green (alias for primary in success contexts). */
val SuccessGreen = Color(0xFF00C48C)

// ─── Text Colours ─────────────────────────────────────────────────────────────

val OnSurfaceDark = Color(0xFFE8EDF5)
val OnSurfaceVariantDark = Color(0xFF9AADBE)
val OnPrimaryDark = Color(0xFF003728)
val OnErrorDark = Color(0xFFFFFFFF)

// ─── Light Theme Counterparts (minimal — app is dark-first) ───────────────────

val SurfaceLight = Color(0xFFF4F7FB)
val OnSurfaceLight = Color(0xFF0F1923)
val PrimaryLight = Color(0xFF006B52)
val OnPrimaryLight = Color(0xFFFFFFFF)
val BackgroundLight = Color(0xFFFFFFFF)
